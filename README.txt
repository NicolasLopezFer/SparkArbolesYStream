# Big Data Analytics

Ejercicios realizados para la clase de mineria

## Qué encontrará 🚀

Hay 2 carpetas, 1 de las carpetas (arbol) solo necesita correr las lineas ya que el archivo .csv ya se encuentra ahi. Para la segunda carpeta (stream) es necesario correr un comando adicional en una terminal (nc -lk 9999) esto con el fin de recrear el chat que vamos a estar escuchando

### Pre-requisitos 📋

Es necesario tener instalado: python3, pip3 y el archivo de spark sacado de:

```
http://spark.apache.org/
```